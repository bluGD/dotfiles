hl.config({
  dwindle = {
    preserve_split = true,
  },
})

hl.config({
  scrolling = {
    fullscreen_on_one_column = false,
    column_width = 0.75,
    focus_fit_method = 0,
    direction = "right",
  }
})
